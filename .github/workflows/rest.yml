# workflow name
name: REST Test

# fire on
on:
  [push,pull_request]

# stuff to do
jobs:
  rest-test:
    name: REST Test
    runs-on: ${{ matrix.os-name }}

    strategy:
      matrix:
        # should run on latest explicit ubuntu version
        os-name: [
          # ubuntu-latest,  # ubuntu-22.04
          ubuntu-22.04,
        ]
    steps:
      # checkout commit
      - name: ✔️Checkout commit
        uses: actions/checkout@v4.1.4
      # create tag on baserom repo
      - name: Create Tag on baserom Repo
        uses: actions/github-script@v7.0.1
        with:
          script: |
            const latest = await github.rest.git.getRef({
              owner: 'codemann8',
              repo: 'z3randomizer',
              ref: 'heads/OWMain'
            })
            console.log(latest)
          # github.rest.git.createRef({
          #   owner: context.repo.owner,
          #   repo: 'z3randomizer',
          #   ref: 'tags/v${{ steps.debug_info.outputs.github_tag }}',
          #   sha: latest.sha
          # })
